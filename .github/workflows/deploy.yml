name: Disabled - GitHub Pages workflow

# This workflow has been intentionally disabled. The project is deployed on Vercel.
# Keeping a minimal, manual-only workflow here avoids accidental Pages deployments.

on:
  workflow_dispatch:

jobs:
  noop:
    name: No-op (GitHub Pages disabled)
    if: false
    runs-on: ubuntu-latest
    steps:
      - run: echo "GitHub Pages workflow is disabled. Use Vercel for deployments."
